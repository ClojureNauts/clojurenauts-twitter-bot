;; Make sure that the actual profiles.clj is not put under version control!
;; Consumer key and secret can be found at https://dev.twitter.com/apps/
{:dev {:env {:consumer-key "your consumer key"
             :consumer-secret "your consumer secret"
             :access-token "your access token"
             :access-token-secret "your access token secret"}}}
